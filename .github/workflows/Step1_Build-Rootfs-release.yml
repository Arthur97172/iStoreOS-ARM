name: 💿 Step1_Build-Rootfs-release

on:
  workflow_dispatch:
    inputs:
      build_version:
        description: "请选择构建版本 (latest 为官网最新)"
        required: true
        default: 'latest'
        type: choice
        options:
          - 'latest'
          - '20251205'
          - '20251128'
          - '20251121'
      luci_version:
        description: "选择luci版本"
        required: true
        default: "24.10.4"
        type: choice
        options:
          - '24.10.0'
          - '24.10.1'
          - '24.10.2'
          - '24.10.3'
          - '24.10.4' 
      network_settings:
        description: "请选择初始网络配置"
        required: true
        default: 'static'
        type: choice
        options:
          - 'static'
          - 'dhcp'
      ipaddr:
        description: '请输入管理IP'
        required: true
        default: '192.168.10.1'
      gateway:
        description: '请输入默认网关'
        required: true
        default: '192.168.10.1'

env:
  VERSION: ${{ inputs.luci_version }}

jobs:
  build:
    runs-on: ubuntu-22.04
    # 关键：添加写入权限，解决 403 报错
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: ⚙️ 设置可执行权限
      run: | 
        chmod +x arm64/build24.sh
        chmod +x shell/*.sh

    - name: ⚙️ 设置IP、网关和版本号
      run: |
        CONFIG_PATH="./files/etc/uci-defaults/99-custom.sh"
        echo "当前网络配置路径: $CONFIG_PATH"
        
        # 1. 精准替换版本占位符（不论你选什么版本，都只会替换 __VNAME__）
        # 如果你的脚本中还有其他地方写了“版本号”，可以保留下面这一行
        sed -i "s/版本号/${{ env.VERSION }}/g" "$CONFIG_PATH"
        
        # 2. 替换脚本末尾描述信息的占位符 (解决叠加问题的关键)
        sed -i "s/__VNAME__/${{ env.VERSION }}/g" "$CONFIG_PATH"
        
        # 3. 处理 Banner (如果 banner 文件里也有占位符)
        [ -f "./files/etc/banner" ] && sed -i "s/__VNAME__/${{ env.VERSION }}/g" "./files/etc/banner"

        # 4. 网络逻辑配置 (保持不变)
        if [ "${{ inputs.network_settings }}" = "static" ]; then
           echo "模式：静态IP (旁路由)"
           sed -i "s/192.168.10.1/${{ inputs.ipaddr }}/g" "$CONFIG_PATH"
           sed -i "/uci set network.lan.ipaddr/a \        uci set network.lan.gateway='${{ inputs.gateway }}'\n        uci set network.lan.dns='${{ inputs.gateway }}'" "$CONFIG_PATH"
        fi

        echo "✅ 配置修改完成，脚本末尾预览："
        tail -n 5 "$CONFIG_PATH"

    - name: ⏬ 安装依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev zstd curl unzip tree

    - name: ⏬ 下载 ImageBuilder
      run: |
        BUILD_VER="${{ inputs.build_version }}"
        if [ "$BUILD_VER" = "latest" ]; then
          URL="https://fw0.koolcenter.com/iStoreOS/ib/armsr/istoreos-imagebuilder-armsr-armv8.Linux-x86_64.tar.zst"
        else
          URL="https://github.com/${{ github.repository }}/releases/download/iStoreOS-Imagebuilder/${BUILD_VER}-istoreos-imagebuilder-armsr-armv8.Linux-x86_64.tar.zst"
        fi
        curl -Lf -o imagebuilder.tar.zst "$URL"
        tar --use-compress-program=unzstd -xvf imagebuilder.tar.zst
        mv istoreos-imagebuilder-* imagebuilder

    - name: ⏬ 复制并设置必备文件
      run: |
        cp arm64/{build24.sh,Makefile} imagebuilder/
        cp shell/{prepare-packages.sh,custom-packages.sh} imagebuilder/
        find files/packages/ -name "*.ipk" -exec cp {} imagebuilder/packages/ \;
        
        mkdir -p imagebuilder/files/etc/{uci-defaults,banner1}
        cp files/etc/uci-defaults/99-custom.sh imagebuilder/files/etc/uci-defaults/
        cp files/etc/banner imagebuilder/files/etc/banner1/
        cp files/etc/rc.local imagebuilder/files/etc/
        
        cd imagebuilder
        sed -i 's/# CONFIG_TARGET_ROOTFS_TARGZ is not set/CONFIG_TARGET_ROOTFS_TARGZ=y/' .config
        sed -i "s|CONFIG_TARGET_ROOTFS_SQUASHFS=.*|# CONFIG_TARGET_ROOTFS_SQUASHFS is not set|g" .config
        sed -i "s|CONFIG_TARGET_IMAGES_GZIP=.*|# CONFIG_TARGET_IMAGES_GZIP is not set|g" .config

    - name: 🧱 构建iStoreOS-rootfs
      working-directory: ./imagebuilder
      run: bash ./build24.sh

    - name: 🚀 上传到 Release
      uses: softprops/action-gh-release@v2.2.1
      with:
        tag_name: iStoreOS-${{ env.VERSION }}-RELEASE-${{ inputs.network_settings }}
        body: |
          📌 版本： iStoreOS-${{ env.VERSION }}-RELEASE
          🧩 集成： 包含了基础系统组件、驱动、网络工具、容器支持、LuCI界面及插件等
          👤 用户名： root | 🔒 密码： 无
          🌐 初始IP： ${{ inputs.network_settings == 'static' && inputs.ipaddr || 'DHCP (单网口) / 192.168.10.1 (多网口)' }}
        files: imagebuilder/bin/targets/armsr/armv8/*generic-rootfs.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
